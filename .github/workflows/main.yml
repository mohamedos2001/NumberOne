on:
  push:
    branches:
      - branch1





jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - name: 'Testing Github action'
        run: pwd

      - name: 'After'
        run: ls

      - uses: action/checkout@v3

      - name: 'Before'
        run: ls
  
